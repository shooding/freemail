To include this package from composer update,
add the following to your composer.son

```
"repositories": [
    {
      "type": "vcs",
      "url": "git@bitbucket.org:leadfrog/freemail.git"
    }
  ],
"require": {
"leadfrog/freemail": "dev-master"
}
```